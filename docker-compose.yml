services:
  server:
    build: ./backend   
    container_name: server_container
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - /backend/node_modules   
    environment:
      - MONGO_URI=mongodb+srv://abenayaka:A9eelssSIPCBpGVX@cluster0.mgyo1zg.mongodb.net/Devops?retryWrites=true&w=majority
      - PORT=5000

  client:
    build: ./frontend   
    container_name: client_container
    ports:
      - "5173:5173"
    depends_on:
      - server
    stdin_open: true
    tty: true
    volumes:
      - ./frontend:/app
      - /frontend/node_modules


